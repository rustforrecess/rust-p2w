# `+` on two strings is concatenation, and `*` by an int repeats.
#
# Here so that a checker written by someone thinking in Rust or Java does not
# quietly outlaw ordinary Python.

greeting = "hello" + " " + "world"
line = "-" * 20

print(greeting)
print(line)
