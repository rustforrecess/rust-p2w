# A beginner writes no annotations at all. This must keep working.
#
# Annotations are a way to say what you mean, learned later. They are not a
# fee charged at the door. If typing makes this program require `x: int`,
# typing has made the language worse for the people it is for.

x = 5
y = x + 3
print(y)
