# Square brackets on something that has no elements.
#
# Common when a student thinks a variable holds a list because an earlier
# version of their program did. WANTED: name what `score` actually is.

score = 42
print(score[0])
