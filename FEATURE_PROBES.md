# Feature probes

**Generated by `tests/upstream_gap.rs` — do not edit by hand.** Every row is a program that was compiled, not a keyword that was searched for. A hand-maintained list of what a compiler supports is wrong the week after it is written, and the first version of this table was wrong on the day: it missed features that are DESUGARED (a lambda becomes a `def`, so there is no node to grep for) and reported inheritance as missing because the probe used `pass` in a class body.

Regenerate with `P2W_BLESS=1 cargo test --test upstream_gap`. **A row moving is a feature landing or a regression** — read the diff.

See `UPSTREAM_GAP.md` for what these gaps mean and which of upstream p2w's designs are worth taking.

## Rejected (31)

| feature | message |
|---|---|
| `with / context manager` | expected a new line, but found `open` |
| `match / case` | expected a new line, but found `x` |
| `walrus :=` | expected ')', but found `:` |
| `yield / generator function` | expected a new line, but found the number 1 |
| `try / except` | expected a value, found Newline |
| `try / finally` | expected a value, found Newline |
| `raise` | expected a new line, but found `ValueError` |
| `assert` | expected a new line, but found the number 1 |
| `global` | expected a new line, but found `x` |
| `nonlocal` | expected a new line, but found `x` |
| `is / is not` | expected ',' or ')' in call, found Name("is") |
| `del name` | `del` removes an item like `del xs[0]` or `del d[key]` — deleting a whole variab… |
| `for ... else` | expected a value, found Newline |
| `while ... else` | expected a value, found Newline |
| `pass in a class body` | a class body can only contain methods (def) and variable assignments |
| `decorator` | unexpected character '@' |
| `*args` | expected a parameter name, found Star |
| `**kwargs` | expected a parameter name, found DoubleStar |
| `multiple inheritance` | multiple inheritance isn't supported — one base class only |
| `bind a function to a name` | 'f' is a function — call it with f(...) |
| `pass a function as an argument` | 'd' is a function — call it with d(...) |
| `return a closure` | expected ',' or ')' in call, found LParen |
| `map / filter` | unknown name 'abs' (define it with `abs = ...` first) |
| `sorted(key=...)` | unknown name 'len' (define it with `len = ...` first) |
| `implicit string concatenation` | expected ')', but found a piece of text (a string) |
| `bytes literal` | expected a new line, but found a piece of text (a string) |
| `integer past 2^31` | the number 4000000000 is too big — whole numbers from -2147483648 to 2147483647 … |
| `percent formatting` | this operator needs numbers on both sides |
| `divmod` | unknown function 'divmod' |
| `type()` | unknown function 'type' |
| `isinstance()` | unknown name 'int' (define it with `int = ...` first) |

## Compiles (11)

- `from X import Y`
- `inheritance`
- `super()`
- `lambda`
- `f-string`
- `generator expression`
- `closure capture`
- `annotated local, only read`
- `del item`
- `chained comparison`
- `keyword args at a call`

## `__slots__`

Declared slots are **NOT enforced** — a class that declares `__slots__` and then takes an undeclared attribute is accepted, so the declaration is being read as an ordinary class variable. "It compiles" is the wrong signal here; the right one is whether the extra attribute is REFUSED.
